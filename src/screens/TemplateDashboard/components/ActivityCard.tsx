import { ActivityInstance } from "../../../services/activity.model";

export interface ActivityCardProps {
  activityTemplate: ActivityInstance;
}

export const ActivityCard = (): JSX.Element => {
  return (
    <></>
  );
};